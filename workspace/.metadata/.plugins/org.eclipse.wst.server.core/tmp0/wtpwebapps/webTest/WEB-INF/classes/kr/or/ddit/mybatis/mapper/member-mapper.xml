<?xml version="1.0" encoding="UTF-8"?>
<!-- 이 문서는 mybatis에서 처리할 SQL문을 작성하는 문서입니다... -->
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="member">

	<select id="getMember" parameterType="memVo" resultType="memVo">
		select * from mymember 
		where mem_id = #{mem_id} and mem_pass = #{mem_pass}
	</select>
	
	<select id="getAllMember" parameterType="memVo" resultType="memVo">
		select * from mymember
	</select>
	
	<insert id="insertMember" parameterType="memVo">
		insert into mymember(mem_id, mem_pass, mem_name, mem_tel, mem_addr)
		values(#{mem_id}, #{mem_pass}, #{mem_name}, #{mem_tel}, #{mem_addr})
	</insert>
	
	<select id="idCheck" parameterType="string" resultType="string">
		select mem_id from mymember where mem_id = #{data}
	</select>
	
</mapper>
